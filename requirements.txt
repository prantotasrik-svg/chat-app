# Core web framework
Flask==2.3.3

# For real-time WebSocket communication
Flask-SocketIO==5.3.6

# Core dependency of Flask
Werkzeug==2.3.7

# Production WSGI server (for Render)
gunicorn==21.2.0

# Async server for SocketIO (gevent: primary, supports Python 3.13+)
gevent>=24.9.0  # FIXED: Latest for Python 3.13 compatibility (fixes Cython 'long' error)

# Cython for gevent build (ensures modern version)
Cython>=3.0.0

# Fallback async (eventlet if gevent fails)
eventlet>=0.36.1

# Environment variables
python-dotenv==1.0.0

# Stable production server (optional fallback)
waitress==2.1.2
